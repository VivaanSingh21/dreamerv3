# py3.7 environment for worker.py (the CARLA 0.9.6 client side). NO torch, NO jax.
# The vibes-carla96 image already ships an equivalent `carla96` conda env; this is
# the minimal set if you build one from scratch:
#
#   conda create -n carla96 python=3.7.12 -y && conda activate carla96
#   pip install -r requirements-carla96.txt
#   # then make the py3.7 egg (0.9.6 ships only py2.7 + py3.5):
#   cp $CARLA_ROOT/PythonAPI/carla/dist/carla-0.9.6-py3.5-linux-x86_64.egg \
#      $CARLA_ROOT/PythonAPI/carla/dist/carla-0.9.6-py3.7-linux-x86_64.egg
#
numpy==1.19.1
pygame==2.6.1
Pillow==9.5.0
dotmap==1.3.30
